# Example "even" public SSH key file
# This file would contain even's public SSH key used for forced commands
# Comments aren't actually allowed, so don't keep them in!
ssh-rsa INVALID-SSH-KEY granting-service
ssh-rsa INVALID-SSH-KEY granting-service
